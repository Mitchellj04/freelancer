import React, { useEffect, useState } from 'react'
import TaskContainer from '../Container/TaskContainer'
import CreateTask from '../Create/CreateTask'

const TaskList = ({list, projects}) => {



  // *****  DELETE THIS FILE ******
  



  
  
  // console.log(tasks)
  // useEffect(() => {
  //   fetch('http://localhost:9292/tasks')
  //   .then (resp => resp.json())
  //   .then (task => {
  //       setTasks(task)
  // }) }, [])

// handleTaskDelete={handleTaskDelete}

  // const taskList = tasks.map((listTasks) => <TaskContainer id={listTasks.id} listTasks={listTasks} key={listTasks.id}  list={list}/>)
  return (
    <>
      {/* {taskList} */}
    </>
  )
}

export default TaskList